fastapi>=0.115
uvicorn[standard]>=0.32
sympy>=1.13
pydantic>=2.9
websockets>=13

# Optional: only needed for LLM-written explanations on the incorrect branch.
# Without ANTHROPIC_API_KEY set, the deterministic explainer is used instead.
anthropic>=0.116

# Tests
pytest>=8.0
httpx>=0.27
